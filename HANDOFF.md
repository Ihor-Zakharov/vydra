# Где остановились (2026-09-24, ~21:10)

Запушено на GitHub. Веб-интерфейс приведён в рабочий минимум: стили совпадают с разметкой,
тесты зелёные (227), скриншоты Edge headless 1440 и 520 px в порядке. Починено: индикатор «0%» в простое,
полоса прокрутки в поле ссылки, раскладка счётчиков хранилища на узком экране.

## ПЛАН (решение пользователя 24.09, главнее всего ниже)
1. **Нормальный UI + стабильная работа в WSL.** Всё остальное потом.
2. Тёмная тема = чёрная дыра, как в KOCMOC (cosmos.js). Это основная и единственная тема на ближайшее время.
3. Светлая тема — в самом конце, после всего остального.

## Приоритеты пользователя (строго по порядку)
1. Надёжная загрузка YouTube / TikTok / Instagram.
2. Обрезка (отрезок).
3. Только звук (MP3) или видео + звук (MP4), либо оба.
4. Видно, куда сохранилось (+ открыть / показать в папке).
5. Смена папки-хранилища вместе со структурой YouTube/TikTok/Instagram/… (+ «перенести уже скачанное?»).
6. Консольная версия «до ума».
Потом: красивый UI в стиле KOCMOC + KOCMOC UNLEASHED (профессионально, но кинематографично; кинотеатр как функцию — убрать).

## Готово и проверено
- Бэкенд: вопросы «не выходит → вот вариант → продолжить?» (downloader.py `_review`, `_download_resilient`; jobs.py статус `waiting`,
  `JobManager.answer`; POST /api/jobs/{id}/answer, `auto_accept`), тесты tests/test_questions.py.
- Прогон реальных сценариев (scratchpad/scen/report.txt): YouTube shorts/обычное/3-часовое MP3/отрезок, TikTok MP4/MP3,
  Instagram MP4+MP3/MP3-отрезок — всё DONE, длительности и кодеки верны. Баг с /answer найден и исправлен.
- Установщики: источник — zip-архив GitHub (Git не нужен), шаг uv больше не молчит. Нативная Windows проверена (44 с).
- У пользователя: стабильная версия с GitHub (`vydra`, `выдра` в WSL и PowerShell через мостик в %LOCALAPPDATA%\vydra\bin),
  ярлык «Выдра» на рабочем столе, сервер http://localhost:8765 (UI в нём сломан).

## Не доделано
- Агент бэкенда остановлен посреди удаления кинотеатра: tests/test_cli.py и tests/test_library.py импортируют CINEMA —
  поправить тесты/cli.py. Не сделаны: `выдра` entry point, русские флаги (-ф мп3), ссылка из буфера, вопросы в CLI,
  перенос хранилища (Library.move_root + POST /api/settings/library {move}), мостик `vydra bridge`, авто-кавычки в оболочках, CI.
- Fable (UI) остановлен посреди редизайна: vydra/static/styles.css не соответствует новым index.html/app.js/explorer.js.
  Сначала — рабочий минимум (ссылка → формат → отрезок → скачать → «куда сохранилось» → смена папки), потом красота.
- Перед пушем: тесты зелёные + скриншоты UI (Edge headless) + чистая установка.
